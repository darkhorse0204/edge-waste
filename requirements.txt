# Core runtime deps for Stage 1 (camera-only waste classifier).
# For GPU: install a CUDA build of torch/torchvision from https://pytorch.org
# before installing the rest, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.2
torchvision>=0.17
timm>=1.0
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
opencv-python>=4.8
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8

# Detection stage (item localization) — needs a release that includes
# YOLO26 (Jan 2026 or later); pin/verify the exact version against
# https://docs.ultralytics.com/models/yolo26 if `yolo26n.pt` isn't recognized.
ultralytics>=8.4

# Optional: only needed to auto-download public datasets for the class merge.
kaggle>=1.6

# Stage 2/3: explainability (Grad-CAM) and edge export. OCI + federated
# learning simulation reuse the core deps above (sklearn, numpy, torch) —
# nothing extra to install.
grad-cam>=1.5
onnx>=1.15
onnxruntime>=1.17
