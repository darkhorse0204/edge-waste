"""Edge-deployed waste classification (Stage 1: camera-only classifier).

See PLAN.md and docs/ for the full staged blueprint. This package currently
implements Stage 1: dataset consolidation, a ConvNeXt + ViT hybrid vision
model, training, evaluation, and an inference demo.
"""

__version__ = "0.1.0"
